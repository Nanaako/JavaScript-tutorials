let kitties = ['tosia', 'terminator', 'hiha'];
//overwrite the value of terminator - [1]
//kitties [1] = 'jako';
//console.log(kitties[1])

//let ages = [5, 100, 20];
//console.log(ages[1]);

// array methods
// join - joining by putting comma, - + etc.
//let result = kitties.join(',')

//let result = kitties.indexOf('tosia');

//concatination=joining things together
//let result = kitties.concat(['tosia', 'terminator']);

// push - pushes new value
let result = kitties.push('tosia');

// pop - takes off the last value
result = kitties.pop()
console.log (result);